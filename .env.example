# WhatsApp OTP: set FLEXGREW_API_KEY in karnalix/settings.py (do not commit the real key).

# OTP channels for unknown hosts (e.g. localhost): sms_only | email_whatsapp (default email_whatsapp).
# OTP_DOMAIN_POLICY_DEFAULT=email_whatsapp
